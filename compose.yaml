services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: icecold
      POSTGRES_USER: icecold
      POSTGRES_PASSWORD: icecold
    ports:
      - "55432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
